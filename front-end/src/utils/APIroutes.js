// Set host URL
const host = 'http://localhost:8080';

// Set URL for getting questions
export const getQuestions = `${host}/game`;

// Set URL for posting score
export const postScore = `${host}/game`;
